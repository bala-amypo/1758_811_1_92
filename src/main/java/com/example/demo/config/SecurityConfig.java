package com.example.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // No security configuration
    // This avoids Spring Security dependency errors
}
